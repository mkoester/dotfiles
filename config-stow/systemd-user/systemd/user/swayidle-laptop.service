[Unit]
# NO `Requisite=graphical-session.target` — it was here until 2026-09-21 and made this unit
# unstartable on Hyprland. Only `niri-session` reaches that target; plain Hyprland (no uwsm
# installed) never does, which is why DMS and kanshi are compositor-spawned too — see the
# Autostart block in hyprland.lua. Requisite means "fail unless already active", so
# `systemctl --user enable --now` failed with "Unit graphical-session.target not found".
# PartOf/After stay: both are inert when the target is not active, and on niri they still give
# the correct stop-with-the-session behaviour. WantedBy below likewise still autostarts it
# under niri; on Hyprland the compositor starts it explicitly.
PartOf=graphical-session.target
After=graphical-session.target

[Service]
# Laptop idle policy — locks EARLY (mobile; in case I walk away or forget). Tune the seconds.
#   120s  backlight to 0 (internal panel via brightnessctl). Restored on resume.
#   300s  RELIGHT the panel, then hyprlock (fingerprint, or password, to return).
#   600s  monitors off (DPMS). Internal eDP has no DP-link/waybar issue.
# THE DPMS TIER IS COMPOSITOR-AGNOSTIC, and has to be: it was `niri msg action …` alone, which
# is a silent no-op under Hyprland (no niri socket), so after the migration this tier simply
# stopped happening — swayidle reports nothing, the screen just never powers off. hyprctl is
# tried first and falls back to niri, so one unit serves a machine running either.
# Enable exactly ONE swayidle-* unit per machine (this one on laptops).
# See Workstation-Documentation/desktop/waybar-supervision.md.
# NOTE: hyprlock has no `-f` (fork) flag, unlike swaylock. swayidle waits for the
# before-sleep command to RETURN while holding the sleep inhibitor, so a bare `hyprlock`
# would block suspend until you unlocked. It is therefore backgrounded, with a 1s guard so
# the lock is actually up before the inhibitor is released. The `pidof` test keeps a second
# instance from stacking when a timeout lock already fired.
# A LOCKED SCREEN MUST BE VISIBLE. The dim tier above sets the backlight to 0, so without
# this the lock prompt appears on an unlit panel — indistinguishable from a dead machine, and
# it can only be recovered if swayidle's `resume` still fires while an ext-session-lock is
# active. Relying on that locked MK out twice on 2026-07-30. The lock tier therefore restores
# the backlight FIRST, so locking always leaves a readable prompt.
# THE LOCKER MUST NOT BE A CHILD OF THIS UNIT. Launched directly, hyprlock lands in this
# service's cgroup, so `systemctl --user stop swayidle-…` KILLS IT WHILE IT HOLDS THE SESSION
# LOCK. ext-session-lock-v1 then requires the compositor to stay locked with no client left to
# authenticate against — a blank screen with a live cursor and no way in. That happened on
# mkMac2014 2026-07-30, caused by the very command meant to be the escape hatch.
# systemd-run puts it in its own transient unit instead, which also removes the need to
# background it for before-sleep: systemd-run RETURNS once the unit is started, so swayidle's
# sleep inhibitor is released without waiting for the unlock. (The user manager already
# exports WAYLAND_DISPLAY/NIRI_SOCKET/XDG_RUNTIME_DIR, so no --setenv is needed.)
# Recovery if it ever happens again: run that same systemd-run line by hand — niri logs
# "locking session (replacing existing dead lock)" and accepts the new client.
ExecStart=/usr/bin/swayidle -w \
  timeout 120 '%h/.config/systemd/user/screen-blank.sh dim internal' resume '%h/.config/systemd/user/screen-blank.sh restore internal' \
  timeout 300 '%h/.config/systemd/user/screen-blank.sh restore internal; pidof hyprlock >/dev/null || systemd-run --user --collect --unit=hyprlock-idle /usr/bin/hyprlock' \
  timeout 600 'hyprctl dispatch dpms off 2>/dev/null || niri msg action power-off-monitors' resume 'hyprctl dispatch dpms on 2>/dev/null || niri msg action power-on-monitors' \
  before-sleep 'pidof hyprlock >/dev/null || systemd-run --user --collect --unit=hyprlock-idle /usr/bin/hyprlock --immediate-render; sleep 1'
Restart=on-failure

[Install]
WantedBy=graphical-session.target
